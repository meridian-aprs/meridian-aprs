/// Application version — single source of truth for the version string.
///
/// Bump this (and pubspec.yaml) on every release. The major version digit
/// drives [AprsIdentity.tocall] automatically.
library;

const kAppVersion = '0.1.0';
